# Session 2026-07-23 - next-step.diff documentation sync

## Summary

Applied the intended contents of `docs/diffs/next-step.diff` manually because
`git apply --check` reported a malformed patch at line 42.

The diff only targeted `TASKS.md`. Before updating the task list, the repo was
checked to confirm the referenced fixes already exist in code:

- `createCreditLot()` accepts an optional `dbOrTx` transaction handle in
  `packages/server/src/modules/credit/services/credit.service.ts`.
- `recordPayment()` passes its own transaction into `createCreditLot()` for
  overpayment-to-credit handling in
  `packages/server/src/modules/documents/services/payment.service.ts`.
- Auto-converted savings deposits are visually distinguished in
  `packages/client/src/pages/party/PartyDetailPage.tsx` with a `(converti)`
  badge when `agentId == null`.

## Documentation Updates

- Marked the Sprint 4 overpayment-to-credit cross-transaction hardening item
  as resolved in `TASKS.md`.
- Marked the Sprint 9 auto-converted epargne deposit labeling item as resolved
  in `TASKS.md`.
- Kept Sprint 9 credit-conversion fee-pair verification open.
- Updated exploration-cache active notes, quick reference, and changelog to
  remove stale references to those resolved hardening items.

## Validation

- Code changes were not needed for this patch; the referenced code fixes were
  already present.
- `git apply --check docs/diffs/next-step.diff` failed with
  `corrupt patch at line 42`, so the changes were applied manually.

## Later 2026-07-23 - Aide, guide triggers, and company parties

Documented the six `feat: update application` commits that landed after the
hardening-status sync.

### What shipped

- Added an in-app Aide section at `/aide`, backed by static Markdown imported
  through Vite `?raw`.
- Added `AideMarkdownView`, a shared Markdown-ish renderer for the help
  content.
- Added help topics for Parties, Documents, Paiements, Creances, Stock,
  Commissions, Epargne, Dashboard/Analyse, Parametres, Utilisateurs,
  Notifications, Journal d'audit, Historique emails, and technical error
  codes.
- Added a right-side contextual `HelpPanel` using the new shadcn-style
  `Sheet` component.
- Extended `PageHeaderProvider` with contextual `helpTopic` and lightweight
  guide steps, then wired guide/help triggers into major pages and document
  create/detail flows.
- Added company-party support: `party_type` enum, `partyType`, `tradeName`,
  and `taxId` fields on `parties`, with migration
  `20260723070400_sleepy_madame_web`.
- Updated party create/edit schemas, dialogs, API types, backend validation,
  and party detail/profile display for individual vs company parties.
- Updated document snapshots and PDF rendering so company parties print the
  trade name and optional RCCM/NIF instead of only the individual contact name.

### Notes

- Aide content is intentionally bundled client-side for now, not DB-backed.
  Editable/admin-managed help remains a later product decision if content must
  change without redeploy.
- Company-party validation requires `tradeName` for `partyType = company` and
  clears company-only fields when switching back to individual.

## Later 2026-07-23 - Dark-mode retrofit close-out

Documented the six `feat: update application` commits that landed after the
Aide/company-party documentation sync.

### What shipped

- Reworked the client design-token layer in
  `packages/client/src/styles/index.css` with a real brand-gold primary token,
  dark-mode variants, tinted dark surfaces, text tiers, and semantic status
  groups for success/warning/danger/info.
- Retrofitted shared shadcn-style primitives to semantic tokens: Button,
  Input, Label, Select, Calendar, DatePicker, Popover, Switch, Tabs,
  `DataTable`, and `read-only-table`.
- Retrofitted the first page cluster around documents and parties: Proformas,
  Invoices, document workspace/detail/create flows, line composers, party
  create/edit/detail/grid/KPI surfaces, savings dialogs, and payment dialogs.
- Retrofitted the remaining operational pages: Dashboard, Analyse tabs,
  Creances, Stock, Commissions, commission edit queue, Users, employee detail,
  Notifications, Mail outbox, Audit log, Aide, and Settings.
- Polished Settings' sensitive-impact cards so the dashed backgrounds now use
  CSS custom properties instead of fixed hex/Tailwind colors.
- Closed the deferred `TASKS.md` dark-mode retrofit item with validation notes
  for typecheck/build and real diff application against a fresh clone.

### Notes

- This was mostly a class/token migration, so feature behavior should remain
  unchanged.
- Runtime visual smoke is still useful on a running app, especially dense
  mobile screens, charts, modals, and dark/light theme switching.

## Later 2026-07-23 - Theme selector and Notion sync

Documented the four `feat: update application` commits that landed after the
dark-mode retrofit documentation sync, then updated the Notion project
dashboard/backlog to match.

### What shipped

- Expanded `ThemeProvider` with persisted independent theme preferences:
  `prestix_theme`, `prestix_dark_variant`, and `prestix_light_variant`.
- Added dark palette variants in Settings > Apparence: Teal, Bleu ardoise,
  and Violet profond.
- Added light palette variants in Settings > Apparence: Neutre, Chaleureux,
  and Frais.
- Kept dark and light palette state independent through `data-theme` and
  `data-light-theme`, so switching modes does not overwrite the saved palette
  for the other mode.
- Refined the CSS palette blocks so light variants can adjust the brand accent
  while dark variants keep the gold accent consistent.
- Adjusted the Login page logo/title layout after the theme work.

### Notion updates

- Updated `PrestiX - Tableau de Bord Projet` so the next action now points
  back to Sprint 12 runtime/UAT instead of planning the theme selector.
- Marked `[Nouvelle fonctionnalite] Selecteur de theme dans Parametres` as
  `Termine` and replaced its body with the implementation summary.
- Updated the Sprint 12 runtime UI smoke task to include theme palette checks.
- Updated the older Settings backlog note so it no longer says remaining pages
  are hardcoded/non-dark-aware.

### Notes

- Runtime smoke still needs to cover Settings > Apparence, Login, dense pages,
  modals, tables, charts, mobile, and palette switching in both modes.
