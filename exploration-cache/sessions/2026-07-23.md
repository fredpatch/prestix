# Session 2026-07-23 - next-step.diff documentation sync

## Summary

Applied the intended contents of `docs/diffs/next-step.diff` manually because
`git apply --check` reported a malformed patch at line 42.

The diff only targeted `TASKS.md`. Before updating the task list, the repo was
checked to confirm the referenced fixes already exist in code:

- `createCreditLot()` accepts an optional `dbOrTx` transaction handle in
  `packages/server/src/modules/credit/services/credit.service.ts`.
- `recordPayment()` passes its own transaction into `createCreditLot()` for
  overpayment-to-credit handling in
  `packages/server/src/modules/documents/services/payment.service.ts`.
- Auto-converted savings deposits are visually distinguished in
  `packages/client/src/pages/party/PartyDetailPage.tsx` with a `(converti)`
  badge when `agentId == null`.

## Documentation Updates

- Marked the Sprint 4 overpayment-to-credit cross-transaction hardening item
  as resolved in `TASKS.md`.
- Marked the Sprint 9 auto-converted epargne deposit labeling item as resolved
  in `TASKS.md`.
- Kept Sprint 9 credit-conversion fee-pair verification open.
- Updated exploration-cache active notes, quick reference, and changelog to
  remove stale references to those resolved hardening items.

## Validation

- Code changes were not needed for this patch; the referenced code fixes were
  already present.
- `git apply --check docs/diffs/next-step.diff` failed with
  `corrupt patch at line 42`, so the changes were applied manually.

## Later 2026-07-23 - Aide, guide triggers, and company parties

Documented the six `feat: update application` commits that landed after the
hardening-status sync.

### What shipped

- Added an in-app Aide section at `/aide`, backed by static Markdown imported
  through Vite `?raw`.
- Added `AideMarkdownView`, a shared Markdown-ish renderer for the help
  content.
- Added help topics for Parties, Documents, Paiements, Creances, Stock,
  Commissions, Epargne, Dashboard/Analyse, Parametres, Utilisateurs,
  Notifications, Journal d'audit, Historique emails, and technical error
  codes.
- Added a right-side contextual `HelpPanel` using the new shadcn-style
  `Sheet` component.
- Extended `PageHeaderProvider` with contextual `helpTopic` and lightweight
  guide steps, then wired guide/help triggers into major pages and document
  create/detail flows.
- Added company-party support: `party_type` enum, `partyType`, `tradeName`,
  and `taxId` fields on `parties`, with migration
  `20260723070400_sleepy_madame_web`.
- Updated party create/edit schemas, dialogs, API types, backend validation,
  and party detail/profile display for individual vs company parties.
- Updated document snapshots and PDF rendering so company parties print the
  trade name and optional RCCM/NIF instead of only the individual contact name.

### Notes

- Aide content is intentionally bundled client-side for now, not DB-backed.
  Editable/admin-managed help remains a later product decision if content must
  change without redeploy.
- Company-party validation requires `tradeName` for `partyType = company` and
  clears company-only fields when switching back to individual.
