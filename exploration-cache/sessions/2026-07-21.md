# Session 2026-07-21 — Sprint 11c-3 Close-out

## Summary

Closed Sprint 11c-3 (UI Hardening: Architectural Migration) in full — the
largest single-sprint push of the UI hardening initiative. Work landed in 16
incrementally-validated diff batches, each independently confirmed via
`git apply --check` + `npm install` + `npx tsc --noEmit` against a fresh
clone before handoff.

## What shipped

**RHF dialogs (carried over from a prior session, verified/corrected here):**
`CreatePartyDialog`, `EditPartyDialog` (shared `party-schema.ts`),
`CreateStockArticleDialog`, `RecordPaymentDialog`, `CreateCommissionDialog`.

**Generic table components:** `ReadOnlyTable` and `DataTable`, initially on
raw `<table>` markup, then rebuilt on shadcn's `Table` primitives once that
component was added mid-sprint. `ReadOnlyTable` grew a `ReactNode` title, an
optional `footer` slot, and a `bare` mode as real usage surfaced needs beyond
the original design.

**React Query hooks migration** — the bulk of the sprint:
- `hooks/queries/` + `hooks/mutations/`, one hook per query/mutation.
- Every page's data-fetching migrated: `PartyDetailPage`, `InvoiceDetailPage`,
  `ProformaDetailPage`, `CommissionEditQueuePage`, `CommissionsPage`,
  `CreancesPage`, `UsersPage`, `EmployeeActivityDetailPage`, `PartiesPage`,
  `InvoicesPage`, `ProformasPage`, `StockArticlesPage`, `DashboardPage`, and
  6 `analyse/*Tab.tsx` files.
- ~15 dialog mutations converted from plain async+manual-invalidate to real
  `useMutation`, activating the previously-dead global error toast.
- Every genuine list/detail table migrated to `DataTable`/`ReadOnlyTable`.

## Bugs found and fixed along the way

- **`UsersPage` OTP-reset and `CommissionsPage` delete had no error handling
  at all** — unhandled promise rejections on failure, silently swallowed.
  Surfaced by the `useMutation` migration; both now toast correctly.
- **Stale handoff claim corrected**: an earlier session's memory/handoff said
  "11 pages migrated to React Query." Actual count was 8, and 3 dialogs
  described as "migrated in sandbox" had never been committed — sandbox
  containers are ephemeral and don't persist between chat sessions. All
  redone and verified.
- **Process bug on my end, twice**: generated a diff from a stale local
  working copy that still had an already-merged change staged, causing a
  patch-apply conflict on Fred's machine. Root-caused and fixed by always
  pulling into a *fresh* clone immediately before generating any diff, never
  reusing a working copy across turns.
- **Self-caught regression before shipping**: an `IssueInvoiceDialog` hook
  draft would have offered the manager-only stock-override UI to any role,
  not just manager+. Caught during the caller review and fixed by threading
  `canOverrideStock` into the hook rather than deciding inside it.

## Deliberate scope boundaries (not oversights)

- `InvoiceDetailPage`/`ProformaDetailPage` line-items tables — inline
  row-editing state doesn't map cleanly onto `DataTable`.
- `CommissionEditQueuePage`'s per-request comparison grid — a 3-column
  current/proposed diff per card, not a record list.
- `SettingsPage` — sub-component-local loading pattern, skipped since 11c-1.
- `BootstrapPage` — one-time wizard, no cross-component cache-sharing need.

## Validation posture

Every diff typecheck-verified (`tsc --noEmit` clean) before handoff. **Not**
manually smoke-tested in a running app this session — that's the natural
next validation step before calling 11c-3 fully done in practice, not just
in code.

## Next

Sprint 11c is closed. Two unscoped priorities remained at this point: Sprint
11d (Notifications) and Sprint 11e (Journal d'audit).

## Sprint 11e Journal d'audit

- Added standalone admin+ `/audit-log` route and sidebar nav item. This keeps
  the full audit log available to admin+ users without giving them the
  super_admin-only Parametres page.
- Added `AuditLogPage` using the existing `/api/audit-log` backend: user,
  action, entity-type, from/to date filters; paginated `DataTable`; reset
  action; refetch indicator; and metadata popover.
- Renamed the query hook from typoed `useAditLog.ts` to `useAuditLog.ts` and
  updated imports.
- Kept Settings' embedded audit tab smaller (`PAGE_SIZE = 10`) while the
  standalone page uses a dedicated `PAGE_SIZE = 12`.
- Touched UI polish around shared `Button` density and the login card/input
  alignment.
- Validation found `ignoreDeprecations: "6.0"` was invalid for the installed
  TS version, so the root config was restored to the known-good `"5.0"` value
  while keeping `packages/server/tsconfig.json` free of a duplicate override.
- Validation: `npm run typecheck` PASS. `npm run build -w packages/client`
  PASS after elevated rerun for the known Vite/esbuild Windows `spawn EPERM`;
  existing chunk-size warning remains.

## Next

Sprint 11d (Notifications) is the remaining unscoped priority.

## Sprint 11f UI/reporting polish

Closed a large UI/reporting polish slice after the audit-log work.

### Admin UI surfaces

- Reworked Party and User list/detail ergonomics with KPI cards, grid/table
  switching, and richer detail headers.
- Reworked Login into a more branded travel-agency sign-in surface.
- Reworked Commission edit requests, Commissions, Creances, and Stock pages
  with KPI cards, filters, grid/card views, and detail modals where relevant.
- Extended Stock with movement tab, item detail modal, stock value, and
  agency gain index based on supplier price.
- Reworked Dashboard in three passes: summary KPIs, trend panels, recent
  sales, top services/parties/employees, section toggles, and commission-type
  trend visualization.
- Reworked Settings ergonomics: human-readable setting names, descriptions,
  criticality styling, card/grid layout, and removed duplicate audit-log
  entry point from Settings now that `/audit-log` exists.
- Added mobile ergonomics improvements: overlay sidebar, scrollable tabs,
  mobile grid defaults for list-heavy pages, and safer table overflow.

### Proforma and Invoice documents

- `ProformasPage` gained KPI cards, status/search filters, pagination through
  `DataTable`, table/grid switch, latest proforma card, mobile grid default,
  and a quick-view modal.
- `InvoicesPage` now mirrors the Proformas list pattern: KPI cards, status
  and payment filters, table/grid switch, latest invoice, mobile grid default,
  overdue emphasis, and quick-view modal.
- `ProformaDetailPage` and `InvoiceDetailPage` now use a split document
  workspace: operational detail/actions on the left and a paper-style preview
  on the right.
- Added shared `DocumentWorkspace` components for KPI cards, status badges,
  party summary, line cards, empty states, preview toggles, and paper preview.
- Validation: typecheck passed; client builds passed after elevated rerun for
  the known Windows Vite/esbuild `spawn EPERM`; existing chunk-size warning
  remains.

### Reporting exports aligned to upgraded Dashboard

- PDF quick report now includes backend-rendered SVG charts:
  CA brut/gain trend, service trend, commission-type trend, and recent sales.
- Excel export now includes graph-oriented sheets:
  `Graph CA-Gain`, `Graph Services`, `Graph Commissions`, and
  `Ventes recentes`.
- Real-use feedback: ExcelJS `dataBar` conditional formatting caused Excel to
  show a repair prompt and strip the graph layer. Repaired by removing
  conditional formatting entirely and using ordinary numeric columns plus
  static text-bar `- vue` columns.
- Fixed `getRecentSales()` so payment rows resolve client names via related
  invoices, not only direct invoice/commission party IDs.
- Validation: `npm run typecheck` PASS and `npm run build -w packages/server`
  PASS after the reporting export changes.

### Commits pushed

- `0870d10 feat: refine admin UI and dashboard workflows`
- `2c64d26 feat: rework invoice and proforma document views`
- `9ecd434 feat: align dashboard report exports`

### Still pending

- Manual runtime smoke for the new UI flows, especially mobile sidebar/tabs,
  document preview toggles, invoice/proforma quick views, and dashboard
  section toggles.
- Fresh real PDF/Excel export open-test after the Excel repair.
- Full reporting/analyse API-runtime smoke remains open before beta
  confidence.

## Sprint 11d Notifications and Mail Foundation

- Added the in-app notifications module with Drizzle schema/migrations,
  `/api/notifications` routes, unread counts, read-all/read-one, archive, and
  paginated status filtering.
- Added client notification infrastructure: `notification.api.ts`, React
  Query hooks/mutations, `/notifications` route, sidebar entry, unread header
  bell, and a notification-center page for inbox-style triage.
- Fixed the initial notification-center `AUTH_REQUIRED` 500 by reading the
  authenticated JWT `userId` field instead of a non-existent `id` field.
- Added system notification producers for expired proformas, penalty accrual
  summaries, credit auto-conversion/held-for-review events, upcoming invoice
  installments, and commission edit workflow decisions.
- Added SMTP/Gmail-ready mail configuration in `utils/mailer.ts` with
  `MAIL_ENABLED`, `SMTP_HOST`, `SMTP_PORT`, `SMTP_SECURE`, `SMTP_USER`,
  `SMTP_PASS`, and `MAIL_FROM`, while keeping backward-compatible
  `SMTP_PASSWORD`/`SMTP_FROM` fallbacks.
- Added admin-only mail status, test-send, and outbox endpoints under
  `/api/notifications/mail/*`.
- Added `mail_outbox` tracking with pending/sent/failed status, recipient,
  subject, template key, provider message id, source, retry count, and error
  metadata.
- Added Settings-managed mail toggles seeded at startup:
  `mail_enabled`, `mail_automatic_reminders_enabled`,
  `mail_document_auto_send_enabled`, and `mail_sender_name`.
- Updated Settings copy so mail options use readable labels, clearer
  descriptions, module grouping, and warning treatment for behavior-changing
  delivery switches.
- Fixed the SMTP test feedback bug where a successful send could display
  "Erreur lors de l'envoi du test" in a success-colored toast.
- Updated root and server `.env.example` files with Gmail app-password SMTP
  guidance.
- Validation: `npm run typecheck` PASS and root `npm run build` PASS after
  elevated rerun for the known Windows Vite/esbuild `spawn EPERM`; existing
  client chunk-size warning remains.

### Deferred after first pass

- Run the two new notification/mail migrations and restart the server before
  runtime testing this slice.
- Build document-email templates and PDF attachments for invoices, proformas,
  and delivery notes.
- Add automatic reminder sending behind the new mail settings toggles.
- Add a richer mail outbox UI/history with retry controls and delivery
  diagnostics.
