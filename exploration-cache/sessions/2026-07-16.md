## Sprint 8 Commission Divers — Core Build

- Confirmed schema, commission-catalog module (Sprint 1), and QuickAddPartyDialog (Sprint 2) already matched M10 spec exactly — no gaps found before building.
- Added `commission` module: `commission.types.ts`, `commission.service.ts` (create/list/soft-delete), controller, routes at `/api/commissions`.
- Added generic `CommissionDynamicFields` client component interpreting the catalog's fieldSchema convention (`string`/`string?`/`period`/`enum:a,b`) — built once, works for all seeded types and any future super_admin-created type.
- Added `CreateCommissionDialog`, `CommissionsPage` (list + type filter + soft-delete), nav/route wiring.

## Sprint 8 Commission Divers — Runtime Smoke

- Runtime-tested all 6 active types (Mobile Money, Transfert et Change, Visa, Location de Voiture, Hébergement, Assurance Voyage) plus Canal+ after live activation.
- Confirmed Client/référent quick-add flow works from inside the commission entry dialog.
- Confirmed soft-delete (admin+) removes a commission from the default list.
- Confirmed a super_admin-created custom type ("Course du mois") appears correctly in the entry dialog with zero code change.

## Sprint 8 Commission Divers — Note Column + Type Editor

- Added `note` (optional text) as a common column on `commission_transactions` — universal across all types, not per-type fieldSchema. Closed real gap: Transfert et Change, Visa, Canal+ had no way to record transaction context.
- Added `commissionCatalogApi.update()` client method (server-side `updateCommissionType` already supported `fieldSchema` edits since Sprint 1, nothing client-side ever called it).
- Added `EditCommissionTypeDialog` (Settings, super_admin) — add/remove/rename fields, pick kind (text/optional text/period/enum-with-options), serializes back to the fieldSchema string convention.

## Sprint 8 Commission Divers — Correction Approval Workflow

- New table `commission_edit_requests` (+ `commission_edit_status` enum: pending/approved/rejected). Not in the original M10 spec — added per real business need (agent mistake correction without allowing silent direct edits, especially on `commissionAmount`).
- Considered three designs explicitly before building: (A) direct edit fully audited, (B) lock amount only, (C) full approval queue. Chose C per explicit decision.
- Service: `createEditRequest` (agent+, mandatory reason, blocks a second pending request on the same transaction), `approveEditRequest` (admin+, applies exact proposed patch atomically), `rejectEditRequest` (admin+, transaction untouched).
- List endpoint enriches each commission with `pendingEditRequestId` via one bulk query, not N+1.
- Frontend: `RequestCommissionEditDialog` (scoped to date/amount/note only — client/référent and type-specific fields deliberately out of this first pass), `CommissionEditQueuePage` (admin+, before/after diff table, requester name via users lookup, approve/reject with optional rejection note), pending badge on the commission list.

## Sprint 8 Commission Divers — Bugs Found

- Client `CommissionType` type never exposed `fieldSchema` despite the server already returning it — would have made the dynamic-field renderer silently render nothing. Caught before shipping the entry dialog.
- Used `<DialogTrigger asChild>` out of habit (Radix convention) — this project uses Base UI, which doesn't support that prop. Confirmed via grep that every other dialog in the codebase uses the plain `<DialogTrigger><Button/></DialogTrigger>` pattern; fixed to match.

## Sprint 8 Commission Divers — Validation

- `npm run typecheck -w packages/server`: PASS (run after every stage: core build, note+editor, correction workflow).
- `cd packages/client && npx tsc --noEmit`: PASS (same cadence).
- `npm test` (vitest, Sprint 5's penalty accrual suite): PASS throughout, unaffected.

## Sprint 8 Commission Divers — Closed

- Sprint marked closed in TASKS.md (2026-07-16). All 5 original checklist items confirmed via runtime test, plus three beyond-scope additions (note column, type editor, correction workflow) fully built and tested.
- Deferred, not forgotten: hard-delete for unused types (soft-disable confirmed sufficient for now), commission Settings tab filter capability, and correction-request coverage for client/référent/type-specific fields.

## Sprint 9 Savings Core Draft

- Added `/api/savings` backend module with direct subscription, account lookup by party, transaction listing, deposits, manager withdrawals, admin reversals, withdrawal receipt PDFs, and super_admin manual credit-conversion trigger.
- Added `receipt_number` and `reversal_of_transaction_id` to `savings_transactions`, and made `agent_id` nullable for system-originated conversion rows.
- Generated migration `20260716082550_daffy_blacklash` for the savings transaction schema changes.
- Added `REC` counter seeding and widened `getNextNumber()` so standalone savings withdrawals can receive stable receipt numbers.
- Added withdrawal receipt PDF generation using the shared print template and `DOCUMENT_PRINTED` audit logging.
- Added credit-window auto-conversion service and daily cron job:
  - existing savings account: converts expired credit lot into a deposit
  - no savings account: creates account, snapshots inscription fee, and deposits the remainder
  - under-fee credit lot: holds for review and logs audit metadata
- Wired invoice `method: "epargne"` payment to withdraw from the party savings account inside the payment transaction using serializable isolation.
- Filled Party History epargne pagination from savings transactions.
- Added `docs/sprint9-savings-core.diff` as a review artifact for this backend slice.

## Sprint 9 Savings Validation

- `npm run typecheck -w packages/server`: PASS.
- `npm run db:generate -w packages/server`: PASS.
- `npm run build -w packages/server`: PASS.

## Sprint 9 Savings Pending

- Runtime smoke for `/api/savings` subscription/deposit/withdrawal/reversal/receipt/conversion flows.
- Runtime smoke for invoice payment with `method: "epargne"` and insufficient-balance blocking.
- Runtime smoke for credit auto-conversion branches and under-fee hold-for-review audit.
- Runtime smoke for Party History epargne pagination.
- Savings client UI.

## Sprint 9 Savings — Frontend

- Added `savings.api.ts` client, `SubscribeButton`, `SavingsTransactionDialog` (deposit/withdraw), `ReverseSavingsTransactionDialog`.
- Wired épargne balance card + tab into `PartyDetailPage.tsx` (was a Sprint-9 placeholder) — real derived balance, transaction history, receipt download links.
- Confirmed `épargne` was already a selectable payment method in `RecordPaymentDialog` since Sprint 4 — no frontend change needed there, works correctly automatically now that the backend enforces it.
- Added manual credit-conversion trigger to Settings' financial tab (super_admin), same pattern as Sprint 5's penalty accrual trigger.

## Sprint 9 Savings — Runtime Smoke Test (8 scenarios)

1. Direct subscription: PASS.
2. Deposit: PASS, but surfaced real feedback — quantity field questioned (no real "quantity" concept for cash), and inscription fee timing was unclear (fee snapshotted but never actually recorded as money moving).
3. Standalone withdrawal: surfaced a real spec misreading — withdrawal is NOT a routine manager+ action; money only leaves via épargne-as-payment (ticket/shop spend), never direct cash withdrawal.
4. Épargne-as-payment on a real invoice: PASS, including the over-balance-blocked case.
5. Auto-conversion manual trigger: PASS mechanically, but surfaced a labeling gap (converted deposits show as generic "Dépôt").
6-7. Fee-timing question and status/labeling exactness — Fred confirmed both are hardening for a later pass, not blocking.
8. Withdrawal receipt PDF: confirmed good. Fred's guidance: keep the standalone withdrawal mechanism (don't remove it) but treat it as an admin-level exceptional override, not a normal peer action — traceability/proof value for special cases.

## Sprint 9 Savings — Corrections After Smoke Test

- Removed the `quantity` field from the deposit/withdraw dialog — cash deposits don't have a real "quantity," was over-engineered by mirroring invoice-line shape.
- **Inscription fee visibility, Fred's explicit choice**: now recorded as a real deposit+withdrawal pair (nets to zero balance) on BOTH entry paths (direct subscription and credit-conversion's new-account branch) — not just a snapshotted number. Real, visible ledger trail.
- **Withdrawal role gate corrected**: manager+ → admin+, both server route and client dialog, reframed in the UI as a deliberate exceptional override (red styling, explicit warning copy) rather than a routine peer of deposit.
- Generated `docs/savings-fee-and-withdrawal-fix.diff` for this correction pass.

## Sprint 9 Savings — Flagged, Not Closed

Per Fred's explicit instruction to keep tracking rather than assume done:
- Credit-conversion path's fee-visibility fix mirrors the direct-subscription fix in code, but wasn't independently re-verified with the same testing rigor after the correction.
- Auto-converted deposits still show as generic "Dépôt" — needs a visible "Converti" distinction. Logged as a backlog item (Notion write failed with a tool-permission error mid-session — needs retry).

## Sprint 9 Savings — Closed

- Sprint marked closed in TASKS.md (2026-07-16), with the two flagged items explicitly carried forward rather than silently marked done.
- Noted in passing, not this sprint's job: party-history's "commercial" section is still an unfilled Sprint 3 TODO — pre-existing gap, confirmed and flagged, not fixed.
